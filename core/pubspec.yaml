name: dashability
description: "AI Observability Layer for Flutter Apps — CLI and MCP server."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.1

executables:
  dashability: dashability

dependencies:
  args: ^2.6.0
  appium_driver: ^0.7.1
  dart_mcp: ^0.5.0
  vm_service: ^15.0.2
  web_socket_channel: ^3.0.2

dev_dependencies:
  test: ^1.25.0
  lints: ^5.1.0
