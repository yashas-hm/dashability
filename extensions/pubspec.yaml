name: dashability_extensions
description: "Optional in-app instrumentation helpers for Dashability. Zero external dependencies."
version: 0.1.0

environment:
  sdk: ^3.11.1

dev_dependencies:
  test: ^1.25.0
  lints: ^5.1.0
