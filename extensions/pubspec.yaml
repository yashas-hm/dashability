name: dashability_extensions
description: >-
  Optional in-app instrumentation helpers for Dashability. Report custom
  interactions, metrics, and screen changes. Zero external dependencies.
version: 1.0.0
repository: https://github.com/yashas-hm/dashability

environment:
  sdk: ^3.11.1

dev_dependencies:
  test: ^1.25.0
  lints: ^5.1.0
