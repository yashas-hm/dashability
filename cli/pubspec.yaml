name: dashability_cli
description: "CLI and MCP server for Dashability AI observability."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.1

executables:
  dashability: dashability

dependencies:
  dashability_core:
    path: ../core
  dart_mcp: ^0.5.0
  args: ^2.6.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.1.0
